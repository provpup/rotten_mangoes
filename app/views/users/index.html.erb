<h1>Rotten Mangoes</h1>
<h2>Users</h2>
<%= paginate @users %>
<table>
<% @users.each do |user| %>
  <tr>
  <td><%= link_to "Show user", user_path(user) %></td>
  <td><%= link_to "Edit user", edit_admin_user_path(user) %></td>
  <td><%= link_to "Delete", user_path(user), method: :delete %></td>
  <td><%= user.email %></td>
  <td><%= user.firstname %></td>
  <td><%= user.lastname %></td>
  </tr>
<% end %>
</table>